DATABASE_URL=
SUPABASE_URL=
SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_JWT_SECRET=
GEMINI_API_KEY=
DISCORD_WEBHOOK_URL=
PORT=3000
# Where to send a browser that lands on this API server's root URL (e.g. because Supabase's
# Site URL is still pointed here for auth email links) — should be the deployed frontend URL.
FRONTEND_URL=http://localhost:5173
